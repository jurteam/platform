import React from "react";
import Button from "JurCommon/Button";

const TakeOathCancelButton = ({ onClick }) => {
  return <Button onClick={onClick}>Cancel</Button>;
};

export default TakeOathCancelButton;
